import React from 'react'

const WeightSort = (props) => {
  return (
    <div>
      <button onClick={props.handleWeightSort}>SORT BY HAWG WEIGHT (LIL'EST TO BIGGEST)!</button>
    </div>
)
}

export default WeightSort
